apiVersion: v1
kind: Service
metadata:
  name: etmsys-frontend
  namespace: etmsys

spec:
  type: ClusterIP

  selector:
    app: etmsys-frontend

  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
